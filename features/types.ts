const today = new Date();
today.getMonth();

// Simple object with no specific Typescript config
const planet = {
  age: 10000,
  water: true
};

// Simple class where Typescript cannot give reference to methods
class Animal {

}
const cat = new Animal();
